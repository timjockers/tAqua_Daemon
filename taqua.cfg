// tAqua configuration file
// This file is written by tAqua_Web and read and processed by tAqua_Daemon


// Configuration of the relays
// 0: Not occupied
// 1: Relay - valve
// 2: Relay - continuous power, off when valve activated
relayConfig = (1, 1, 1, 0, 0, 0, 0, 2);


// Irrigation time in seconds upon pressing the button
buttonIrrigationTime = 300;


// Scheduled events
scheduled = (
    {   
        relay = 0;        // Relay (0-7)
        start = (3, 300); // Start (Weekday 0-6, Minutes past midnight 0-1439)
        duration = 60;    // Duration (Seconds)
        condition = {};   // Condition (Group used in a later version for, e.g., weather or sensor conditions)
    },
    {
        relay = 1;
        start = (3, 300);
        duration = 120;
        condition = {};
    },
    {   
        relay = 2;
        start = (3, 300);
        duration = 90;
        condition = {};
    }
);
